query getJobs {
    jobInfos {
        jobDefinition {
            id
            name
            runCmd
            runCmdArgs
            customJobLabel
            nextJobDefaultId
            nextJobErrorDefaultId
            nextJobOptions
        }
        jobActivity {
            jobIdentifier {
                id
                name
                jobInstanceId
                resultSeqNum
            }
            logDateTime
            logLevel
            result
        }
    }
}
